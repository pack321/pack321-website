# Founding Organization Mode

Pack 321 is seeded as `org-pack-321` with `founding_organization_mode=1`. This flag enables rollout reporting without changing standard membership/onboarding architecture and can later be disabled through an authorized administrative action.
